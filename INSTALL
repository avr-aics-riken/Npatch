/*
* Npatchlib - Nagata Patch Library
*
* Copyright (c) 2012-2016 Advanced Institute for Computational Science, RIKEN.
* All rights reserved.
*
*/


SOFTWARE REQUIREMENT
====================
- none


HOW TO COMPILE
==============

1) with configure

$ export NPT_HOME=hogehoge
$ cd BUILD_DIR
$ ../configure [options]
$ make
$ make install


Generated archives are;
libNpatch.a/la/dylib



Configure options:

 --enable-shared=(yes|no)
    This flag tells building shared static library. The default is 'yes'.

 --host=hostname
    Specify in case of cross-compilation.

 --prefix=INSTALL_DIR
    Specify a directory to be installed. The default directory is /usr/local/Polylib.

 --with-real=(float|double)
    This option allows to specify the type of real variable. The default is float.


 CXX=CXX_COMPILER
    Specify a C++ compiler, e.g., g++, icpc, xlc++ or others.

 CXXFLAGS=CXX_OPTIONS
    Specify compiler options.




2) Configure Examples

# Intel compiler

## single precision(default)
$ ../configure --prefix=${NPT_HOME}/Npatchlib \
               CXX=icpc \
               CXXFLAGS="-O3"

# GNU compiler

## single precision(default)
$ ../configure --prefix=${NPT_HOME}/Npatchlib \
               CXX=g++ \
               CXXFLAGS="-O3"

## double precision(default)
$ ../configure --prefix=${NPT_HOME}/Npatchlib \
                --with-real=double
               CXX=g++ \
               CXXFLAGS="-O3"

# FUJITSU compiler / FX, K computer

$ ../configure --prefix=${NPT_HOME}/Npatchlib \
               --host=sparc64-unknown-linux-gnu \
               CXX=FCCpx \
               CXXFLAGS="-Kfast,ocl,preex,simd=2,array_private,parallel,openmp"




2. Hand compile

If a provided configure does not work, use Makefile_hand to build this library. At first,
edit MACRO to tell your machine environment.

